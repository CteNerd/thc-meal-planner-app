import { Card } from '../components/ui/Card';

export function CookbookPage() {
  return (
    <Card>
      <h2 className="text-2xl font-semibold text-slate-900">Cookbook</h2>
      <p className="mt-3 text-sm text-slate-600">Protected route scaffold for recipe browsing, detail views, and creation.</p>
    </Card>
  );
}